let limits = {'100': 10, '200': 30, '500': 40}, nominals = [500, 200, 100];

function atm(sum) {
if (sum % 100 != 0) return 'Error: incorrect sum';
let needSum = sum, nominal = nominals[0];
Math.floor()
}
